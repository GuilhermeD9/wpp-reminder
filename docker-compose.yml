services:
  wpp-reminder:
    container_name: wpp-reminder
    build: 
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - wwebjs_auth:/app/.wwebjs_auth
      - wpp_data:/app/data
    stop_grace_period: 30s

volumes:
  wwebjs_auth:
  wpp_data: